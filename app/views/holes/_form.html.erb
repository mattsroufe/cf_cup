<%= form_with(model: hole) do |form| %>
  <% if hole.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hole.errors.count, "error") %> prohibited this hole from being saved:</h2>

      <ul>
        <% hole.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :course_id, style: "display: block" %>
    <%= form.text_field :course_id %>
  </div>

  <div>
    <%= form.label :par, style: "display: block" %>
    <%= form.number_field :par %>
  </div>

  <div>
    <%= form.label :meters, style: "display: block" %>
    <%= form.number_field :meters %>
  </div>

  <div>
    <%= form.label :stroke, style: "display: block" %>
    <%= form.number_field :stroke %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
