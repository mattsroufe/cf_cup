<%= form_with(model: match_team) do |form| %>
  <% if match_team.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(match_team.errors.count, "error") %> prohibited this match_team from being saved:</h2>

      <ul>
        <% match_team.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :match_id, style: "display: block" %>
    <%= form.number_field :match_id %>
  </div>

  <div>
    <%= form.label :team_id, style: "display: block" %>
    <%= form.number_field :team_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
